=============================
WRITTEN BY ME FOR v1.4 Update
=============================
> I have a score & score penalty system (point system), I should be able to create my own systems on the fly (with a reward / penalty) at settings in a new section called "Stats Configuration".
    I can create stats like strength, focus, intelligence etc. you get the idea. I should be able to edit the icon and name of each stats also the button style (colors) when I add the stat to a page. In "Edit Section" for dynamic sections,
    I should have an additional "Enable Stats System" to use my custom stats. Point system stays separate. Since I can add multiple stat reward / penalty to task entires after this system is created,
    I want a smaller card attached with the main card (it appears below it but is not same as sub task which expands the size of card, its an entirely new card but very less in height),
    all my stats rewards / penalty will be displayed there including points which used to show inside the card before. I should be able to hide this or using an "Eye" icon that will be in place of where the score/penalty used to show.
This is a very heavy change so, create phases for this new huge feature addition.

=====================================
GENERATED BY AI FOR THIS HUGE UPDATE
=====================================
Phase 1: Data Architecture & Foundations
Before we build any UI, we need the application to know what a "Stat" is and how tasks interact with them.
Create CustomStat.java: A new model to hold the stat's ID, Name, Icon, Background Color, and Text Color.
Update AppStats.java: Add a global list to store the user's created custom stats, and a map to track their total accumulated XP for each stat.
Update SectionConfig.java: Add the enableStatsSystem boolean flag.
Update TaskItem.java: Add data structures to hold the specific stat rewards and penalties assigned to that specific task.

Phase 2: Settings UI - "Stats Configuration"
We will build the management console so you can actually create and design your stats.
Create StatsManagerPanel.java: A brand new settings panel (similar to your Section Manager) where you can add, edit, delete, and re-order your custom stats. It will include color pickers for the background/text and an icon selector.
Update SettingsModule.java: Inject this new panel into your main settings page.

Phase 3: Task Dialogs & Section Editor
Now we connect the stats to your daily workflow.
Update SectionManagerPanel.java: Add the "Enable Stats System" toggle so you can turn this feature on or off per section.
Update TaskDialogs.java: Modify the "Edit Task" dialog. If the section has the Stats System enabled, we will dynamically generate input fields for every custom stat you've created, allowing you to assign specific reward/penalty values (alongside the existing global points).

Phase 4: Task Card Redesign (The Mini-Card & Eye Icon)
This is the visual overhaul of the main application interface.
Update TaskCard.java: * Remove the old inline Points/Penalty badges.
Inject the "Eye" 👁️ toggle button in that exact spot.
Build the new "Mini-Card" (a sleek, compact HBox or FlowPane attached to the bottom of the main card, but visually distinct from sub-tasks).
Wire the logic so the Eye icon smoothly expands/collapses this mini-card.
Update the handleTaskCompletion logic so that checking off a task safely distributes both the standard Points AND the new Custom Stat XP to your global profile.

======================
CHANGES DONE AFTERWARD
======================
> Instead of expanding the current task item like in screenshot, you should create a smaller task item that is part of the original task item and placed below it.
  This smaller task item holds all the details about the stat reward / penalty.
> It should match the size of the original task card width. Also when I add a subtask, hide/show subtask or create a new task entry, those gets hidden immediately.
  Along with the stats info of other cards. This shouldnt happen and if stats are currently shown, they will still show and not abruptly hide themselves
> Make the fields of "Edit Stat" dialog box all same and consistent, I really like the contrast of text color vs background color of score reward/penalty.
  Add a "Randomize" button below text color which will randomly pick a color and background of similar contrast of score reward/penalty for the stat Im creating
> Add a checkbox in "general configuration" section at settings page, that turns on "Custom Stats" on/off, if custom stats isn't turned on I cannot add anything to Stats Configuration or turn on "enable custom stats system" in edit section config dialog.
  Turning this on also adds a "Current Stats" section in analytics page where my total stats are being tracked.

=======================
NEW ADDITIONS FOR v1.39
=======================
> The custom stats I am able to add in stats configuration should be able to:
    - Keep record of description of the stat
    - Add max cap for the stat
    - Time until stat atrophy
> The Stats shown in "Current Stats" section in Analytics dashboard should
    > Keep record of Total stat point earned in lifetime for that stat
    > Keep record of Total stat point lost in lifetime for that stat
    > Show current amount of the stat
    > Show maximum level reached for that stat
> I should be able to create a new type of page called "Stat page", which is available in "section config" in manage dynamic sections
    - In stat page I'm able to add a new type of card called stat card, which is like any other card except with a counter with current/max amount of that stat
    - The stat card style should be same as the bg color, outline color, text color and icon color as I created in the stat configuration
> I should be able to create a new type of page called "Perk page", which is available in "section config" in manage dynamic sections
    - In perk page I'm able to add a new type of card called perk card, which shows name of the perk and requirements to unlock it, I can also hook multiple task dependency for it to unlock. There is also description of the perk which shows similar to when I expand a sub-task
    - I can hook multiple stat required to unlock it. Perks are lost if a certain stat decreases.
    - A perk increases in glow the more I increase its level if Im able to maintain it every week (e.g. week 1 = level 1, week 2 = level 2, week 3 = level 3, if I dont lose the stats to maintain it), max glow is reached at level 5
> A stat Atrophy (Passive Decay)
    - If a user doesn't complete any tasks associated with a specific stat (like "Strength" or "Creativity") for 3 or 7 days, that stat begins to slowly drain.
    This forces you to maintain a balanced routine to keep your highly-leveled Perks from de-leveling and losing their glow. This atrophy is configurable per stat

=================================================
NEW ADDITIONS FOR v1.39 DIVIVED INTO PHASES BY AI
=================================================